name: Daily English Reader - disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This repository is intended to run with Claude Code Routines, not GitHub Actions."
